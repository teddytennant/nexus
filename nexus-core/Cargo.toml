[package]
name = "nexus-core"
version = "0.1.0"
edition = "2021"
description = "Knowledge graph intelligence engine - core library"

[dependencies]
regex = "1"
walkdir = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
